/*import { useState } from "react";
import { Card } from "react-bootstrap";



function LittleMain() {
    const [isClicked, setIsClicked] = useState(false);
    const [clickedId, setClickedId] = useState(null);

    const handleClick() {
        setIsClicked(true);
    }

    return (
        {isClicked?
                    <Link to={`/tra-loi-${card.id}`}>CustomCard className="CustomCard" {...cards[0]}/> </Link>

                    <Card key={card.id} {...card} /></Link>:
                    
            }

        

        }
    )
}
*/